import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['cf.bstatic.com', 'a0.muscache.com', 'maps.googleapis.com', 'lh3.googleusercontent.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
